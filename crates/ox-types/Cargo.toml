[package]
readme = "README.md"
include = ["src/**", "tests/**", "Cargo.toml", "README.md", "LICENSE", "build.rs", "artifacts/**", "gateway.assembly.yaml"]
publish = ["crates-io"]
name = "ox-types"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
description = "Shared types for the ox agent framework — pure data, serde-ready"

[dependencies]
tracing = { workspace = true }
horns-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
structfs-core-store = { workspace = true }

[dev-dependencies]
ox-kernel = { path = "../ox-kernel" }
structfs-serde-store = { workspace = true }
